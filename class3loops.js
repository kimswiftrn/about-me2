'use strict';

console.log('js linked!');

//practicing loops: countdouwn!

var timeToLiftoff = 10:

//console.log a countadown from 10 to 1, then liftoff!

console.log(timeToLiftoff); // should log 10
timetoLiftoff = timeToLiftoff -1
console.log(timeToLiftoff); // should log 9
timeToLiftoff = timeToLiftoff -1;

// keep doing that 8 more times
//wound be a side piece of code. 

// instead, let's use a loop!
//loop should stop when timeToLiftoff is 0
//so loop should keep going while timeToLiftoff is more than 0

while(while timeToLiftoff >0){
  console.log(timeToLiftoff);
  timeToLiftoff = timeToLiftoff - 1;
}
console.log("liftoff")
// now on to arrays... she will use the square bracket...this is a while loop. 
var fruits = [ '''apple, banana, catalopur, dragonfruit'];
console.log (fruits[0]);
fruits[0]= 'apricot';
console.log(fruits[0]);
var i =0;
while 9i< fruits.length){
  console.log(fruits [i]);
  i=i+1;
  }

  for(var i=0; i<fruits.length;i =i +1)
  {
    console.log(fruits[i]);
}
// save this to remember what she said.
while(/*something, come back to this*?){
  console.log(timeToLiftoff);
  timeToLiftoff = timeToLiftoff - 1;
}